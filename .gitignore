# Dependencies
node_modules/
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Build output
dist/
build/
out/

# Next.js
.next/
next-env.d.ts
.vercel
*.tsbuildinfo

# Backups (CLAUDE.md 백업 규칙)
_bak/
*_bak_*

# OS / Editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json

# Cache
.cache/
*.tmp

# ─────────────────────────────────────────────────────────────
# 공개 저장소 노출 차단 — 홈페이지 작동(빌드·배포)에 불필요한 내부 자료
# 파일은 로컬(OneDrive)에 그대로 남고, git 추적에서만 제외된다.
# ─────────────────────────────────────────────────────────────

# 내부 문서 (보안 점검·설계·개발 규칙) — 공격자에게 구조를 알려줌
SECURITY.md
CLAUDE.md
PROCESS.md
DESIGN.md
DATABASE.md

# Supabase 백엔드 — DB 스키마·RLS 정책·Edge Function
# 공개 시 권한 구조가 그대로 드러나므로 제외 (배포는 Supabase 대시보드에서 수동)
supabase/

# AI 도구 설정
.claude/
