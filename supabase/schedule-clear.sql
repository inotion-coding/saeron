-- ============================================================
--  시간표 데이터 전체 비우기 (테이블 구조·정책은 유지)
--  잘못 올라간 시간표를 정리하고 처음부터 다시 만들기 위함.
--  SQL Editor에 붙여넣고 Run.
-- ============================================================

delete from public.schedule_rows;      -- 수업 행
delete from public.schedule_teachers;  -- 강사 묶음
delete from public.common_notices;     -- 공통 안내

-- 참고: 테이블·RLS 정책은 그대로 유지되므로, 이후 새 구성으로 재사용 가능.
-- ============================================================
